const Nome = document.getElementById("Nome");
const Email = document.getElementById("Email");

function nome(nome){

    if (mensagem_confirmação.innerHTML == "mensagem_confirmação"){
    console.log("mensagem_confirmação ", "Muito obrigado(a), senhor(a)" );
    console.log("Acabamos de enviar um e-mail para:")
    return true;
    }

} 

function botão(botão)
{
    onclick=botão;
}